# SmartReply Landing (Next.js + Tailwind)

This directory contains a dedicated landing page app for SmartReply, built with Next.js and Tailwind CSS.

## Run

```bash
cd landing
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).
